---
layout: post
title: "套取数据教程"
date: 2026-02-25
categories: 技术
author: WYZqaq
---

首先，套取数据的使用场景是在输入和输出的数量都特别少的题（比如输入两个数，输出一个数）的题，这样的题可以套取数据 
那么，如何套取数据呢？

第一个方法：我们知道LOJ上的数据是公开的，所以，我们可以去LOJ上套取数据。（AT半公开）（大部分题都没有）

方法二：我们首先要知道一个特性，洛谷 RE 后会给你程序的返回值，我们可以利用这个特性，程序的返回值即main函数的返回值，所以，我们可以在主函数中return输入的数对255取模，例如
```cpp
#include<bits/stdc++.h>
using namespace std;
int main(){
	int n,m;
	cin>>n>>m;
	return n;
} 
```
这样我们就知道了输入的 `n%255`。
```cpp
#include<bits/stdc++.h>
using namespace std;
int main(){
	int n,m;
	cin>>n>>m;
	return n/255;
} 
```
这样我们就知道了输入的 `n ÷ 255` 。
我们如何获得输出数据呢？最简单的方法就是询问题解。此时，就有聪明的同学要问了，没有题解怎么办？首先，如果你的答案与标准答案不一样的话，就会返回不一样的值，例如在B4299中，我们使用以下代码：
```cpp
#include<bits/stdc++.h>
using namespace std;
int main(){
	int n,m;
	cin>>n>>m;
	cout<<"a"; 
	return 0;
} 
```
在第一个测试点中，就会得到` read a, expected 2 `这样的结果我们就知道输出的第一个数的第一位为2
如果我们使用这份代码
```cpp
#include<bits/stdc++.h>
using namespace std;
int main(){
	int n,m;
	cin>>n>>m;
	cout<<"2a"; 
	return 0;
}
```
第一位就会显示`too long ` 我们就知道第一个测试点为2如此重复，直到知道所有数据。